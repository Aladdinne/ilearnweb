{% extends "base.html.twig" %}
{% form_theme FormCour 'bootstrap_4_layout.html.twig' %}
{% block body %}

    {{ form_start(FormCour, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(FormCour.recaptcha) }}
    {# must be the same name of this put on the FormBuilder #}

    {{ form_errors(FormCour.recaptcha) }}
    <br>
    {{ form_widget(FormCour) }}
    {{ form_end(FormCour) }}





{% endblock  %}